#include <stdio.h>
#include <stdlib.h> 

const char alphabet[26] = "abcdefghijklmnopqrstuvwxyz";

int main (void) {
    int count, count2;

    for (count = 0; count < 26; count++) {
        puts("");
        for (count2 = 0; count2 < 26; count2++) {
        printf ("\nbjh-yvqq-y%c%c", alphabet[count], alphabet[count2]);
        }
    }
    
}